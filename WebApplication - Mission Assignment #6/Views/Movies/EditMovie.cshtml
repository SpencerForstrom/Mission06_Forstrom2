@*
    EditMovie.cshtml
    This page contains a form that allows users to modify details of an existing movie.
    The form fills with existing data and includes validation.
*@
@model WebApplication___Mission_Assignment__6.Models.Movie

<h2 class="text-center">Edit Movie</h2>

<form method="post" asp-controller="Movies" asp-action="UpdateMovie">
    <input type="hidden" name="MovieId" value="@Model.MovieId" />

    <!-- Title -->
    <div class="mb-3">
        <label class="form-label">Title:</label>
        <input type="text" name="Title" class="form-control" value="@Model.Title" required />
    </div>

    <!-- Year -->
    <div class="mb-3">
        <label class="form-label">Year:</label>
        <input type="number" name="Year" class="form-control" value="@Model.Year" required />
    </div>

    <!-- Director -->
    <div class="mb-3">
        <label class="form-label">Director:</label>
        <input type="text" name="Director" class="form-control" value="@Model.Director" required />
    </div>

    <!-- Rating -->
    <div class="mb-3">
        <label class="form-label">Rating:</label>
        <select name="Rating" class="form-select" required>
            <option selected>@Model.Rating</option>
            <option>G</option>
            <option>PG</option>
            <option>PG-13</option>
            <option>R</option>
        </select>
    </div>

    <!-- Edited -->
    <div class="form-group">
        <label asp-for="Edited">Edited</label>
        <select asp-for="Edited" class="form-control">
            <option value="">-- Select an option --</option>
            <option value="true" selected="@(Model.Edited ? "selected" : null)">Yes</option>
            <option value="false" selected="@(Model.Edited ? null : "selected")">No</option>
        </select>
        <span asp-validation-for="Edited" class="text-danger"></span>
    </div>

    <!-- Lent to -->
    <div class="mb-3">
        <label class="form-label">Lent To:</label>
        <input type="text" name="LentTo" class="form-control" value="@Model.LentTo" />
    </div>

    <!-- Notes -->
    <div class="mb-3">
        <label class="form-label">Notes:</label>
        <input type="text" name="Notes" class="form-control" maxlength="25" value="@Model.Notes" />
    </div>

    <button type="submit" class="btn btn-success">Update Movie</button>
</form>
